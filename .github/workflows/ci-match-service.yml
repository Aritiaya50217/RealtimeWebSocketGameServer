name: CI/CD Pipeline - Match Service

# ----------------------------
# Trigger
on:
  push:
    branches: ["main"]
    paths:
      - "match-service/**"
      - ".github/workflows/ci-match-service.yml"

  pull_request:
    branches: ["main"]
    paths:
      - "match-service/**"
      - ".github/workflows/ci-match-service.yml"

  workflow_dispatch:
    inputs:
      service:
        description: "Select service to run manually"
        required: true
        default: "match-service"
        type: choice
        options:
          - match-service

# ----------------------------
jobs:

  # ----------------------------
  # Build & Test Match Service
  match-build-test:
    name: Build & Test Match Service
    runs-on: ubuntu-latest
    if: github.event_name != 'workflow_dispatch' || github.event.inputs.service == 'match-service'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24.x"

      - name: Download dependencies
        run: |
          cd match-service
          go mod tidy
          go mod download

      - name: Run unit tests
        run: |
          cd match-service
          go test ./internal/... -v -race

      - name: Build binary
        run: |
          cd match-service
          go build -v -o ./bin/match-service ./cmd/match

  # ----------------------------
  # Docker Build & Push 
  docker-build-push:
    name: Docker Build & Push Match Service
    runs-on: ubuntu-latest
    needs: match-build-test
    if: github.event_name != 'workflow_dispatch' || github.event.inputs.service == 'match-service'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Match Service Docker
        run: |
          cd match-service
          docker build -t ${{ secrets.DOCKER_USERNAME }}/match-service:latest .
          docker tag ${{ secrets.DOCKER_USERNAME }}/match-service:latest \
                     ${{ secrets.DOCKER_USERNAME }}/match-service:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/match-service:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/match-service:${{ github.sha }}
